---
- name: nextcloud role
  tags: [role-nextcloud-restore]
  block:

  # install
  - ansible.builtin.import_tasks: subtasks/nextcloud-install.yml

  # systemd
  - ansible.builtin.import_tasks: subtasks/nextcloud-systemd.yml
  
  # config
  - ansible.builtin.import_tasks: subtasks/nextcloud-config-restore.yml